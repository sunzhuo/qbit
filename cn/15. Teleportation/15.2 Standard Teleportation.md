# 标准传送

在标准隐形传输协议中，发送者（通常称为 Alice）和接收者（Bob）两方首先共享一对纠缠的量子位。该协议的目标是让 Alice 使用共享的量子位对和经典通信通道将未知的量子态传输给 Bob。

我们将传送协议分为几个步骤：

- 准备（创建发送给 Alice 和 Bob 的纠缠量子位对）。
- 发送消息（Alice 的任务）：将消息量子位与 Alice 的量子位纠缠在一起，并提取两个经典位发送给 Bob。
- 重建消息（鲍勃的任务）：使用鲍勃从爱丽丝收到的两个经典位，使鲍勃的量子位进入消息量子位最初的状态。
- 最后，我们将这些步骤组合成完整的传送协议。
