# 生成任意大小的数字

让我们更进一步，生成一个 $N$ 位数字。

**输入：** 整数 $N$ ($1 \le N \le 10$)。

**目标：** 生成 $[0, 2^N - 1]$ 范围内的随机数，并以相同的概率获得该范围内的每个数字。

> 有用的 Q# 文档：
>
> * <a href="https://learn.microsoft.com/azure/quantum/user-guide/language/expressions/iterations" target="_blank">for 循环</a>
> * <a href="https://learn.microsoft.com/azure/quantum/user-guide/language/typesystem/immutability" target="_blank">可变变量</a>

<details>
  <summary><b>需要提示吗？</b></summary>
  请记住，您可以使用先前定义的操作来实施您的解决方案。为方便起见，<b>RandomBit</b> 操作已可供您在本练习中使用。
</details>

## 解决方案注释

让我们再次重用“生成单个随机位”练习中的 `RandomBit` 操作。
您将通过调用 `RandomBit` 运算 $N$ 次来生成 $N$ 随机位，并将结果视为您要查找的整数的二进制表示法。
由于使用 $N$ 位写入的数字的最大值为 $2^N - 1$，因此您不需要执行任何额外的检查来确保结果在给定范围内。

来源：Solution.qs

```qsharp
namespace Kata {
    operation RandomNBits(N : Int) : Int {
        mutable result = 0;
        for i in 0 .. N - 1 {
            set result = result * 2 + RandomBit();
        }
        return result;
    }

    // You can use the operation defined in the first exercise to implement your solution.
    operation RandomBit() : Int {
        use q = Qubit();
        H(q);
        return MResetZ(q) == Zero ? 0 | 1;
    }
}
```
